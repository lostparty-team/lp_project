import { IconProps } from '@/types/icon';
import { LuClock3 } from 'react-icons/lu';
export default function InfoIcon({ size = 24, color }: IconProps) {
  return <LuClock3 size={size} color={color} />;
}
