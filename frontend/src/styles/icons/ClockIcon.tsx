import { IconProps } from '@/types/icon';
import { GrContactInfo } from 'react-icons/gr';
export default function ClockIcon({ size = 24, color }: IconProps) {
  return <GrContactInfo size={size} color={color} />;
}
